/**
 * Drizzle schema for Camping El Higuerón.
 *
 * Future domain tables (add as pgTable exports here):
 * - users
 * - reservas
 * - formularios
 * - eventos
 *
 * Flow: define tables → pnpm db:generate → pnpm db:migrate
 */

export {}
